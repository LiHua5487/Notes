
Serving 与 Inference 在于，多个用户可能会同时访问一个模型，这会带来巨大的输入量，我们希望低 latency ，高 throughput 

如果一个用户给一个 request ，就立马放到模型里跑，那这期间其它用户的 request 就不能处理了；可以先稍微等一下，看看这期间有没有其它用户输入，如果有的话，就打包到一起给模型，这样延迟会稍微增加，但是吞吐量更大






















