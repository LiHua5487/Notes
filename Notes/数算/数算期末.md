
# 图算法时间复杂度

BFS 和 DFS
- 邻接表：有向图总代价为 $\Theta(\lvert V \rvert + \lvert E \rvert)$ ，无向图为 $\Theta(\lvert V \rvert + 2\lvert E \rvert)$ 
- 邻接矩阵：总代价为 $\Theta(\lvert V \rvert^2)$ 

Dijkstra 算法
- 直接遍历：总时间复杂度为 $\Theta(|V|^2)$ 
- 最小堆：总时间复杂度为 $\Theta((|V| + |E|)\log |V|)$ 或 $\Theta((|V| + |E|)\log |E|)$ （每次取出顶点、每次更新边权均需要调整最小堆）
- 稠密图下，直接遍历的效率可能比最小堆高

Floyd 算法：总时间复杂度为 $O(|V|^3)$

Prim 算法
- 直接遍历：总时间复杂度为 $\Theta(|V|^2)$ 
- 最小堆：总时间复杂度为 $O(\lvert E\rvert \log \lvert V\rvert)$ 

Kruskal 算法
- 总时间复杂度为 $O(\lvert E\rvert \log \lvert E\rvert)$ 
- 适合稀疏图

# 内排序算法复杂度与稳定性

| 算法         | 最大时间                 | 平均时间                 | 最小时间                 | 辅助空间        | 稳定性 |
| ---------- | -------------------- | -------------------- | -------------------- | ----------- | --- |
| 直接插入排序     | $O(n^2)$             | $O(n^2)$             | $O(n)$               | $O(1)$      | 稳定  |
| 二分法插入排序    | $O(n^2)$             | $O(n^2)$             | $O(n \log n)$        | $O(1)$      | 稳定  |
| 冒泡排序       | $O(n^2)$             | $O(n^2)$             | $O(n^2)$             | $O(1)$      | 稳定  |
| 改进的冒泡排序    | $O(n^2)$             | $O(n^2)$             | $O(n)$               | $O(1)$      | 稳定  |
| 选择排序       | $O(n^2)$             | $O(n^2)$             | $O(n^2)$             | $O(1)$      | 不稳定 |
| Shell排序(3) | $O(n^{3/2})$         | $O(n^{3/2})$         | $O(n^{3/2})$         | $O(1)$      | 不稳定 |
| 快速排序       | $O(n^2)$             | $O(n \log n)$        | $O(n \log n)$        | $O(\log n)$ | 不稳定 |
| 归并排序       | $O(n \log n)$        | $O(n \log n)$        | $O(n \log n)$        | $O(n)$      | 稳定  |
| 堆排序        | $O(n \log n)$        | $O(n \log n)$        | $O(n \log n)$        | $O(1)$      | 不稳定 |
| 桶式排序       | $O(n + m)$           | $O(n + m)$           | $O(n + m)$           | $O(n + m)$  | 稳定  |
| 基数排序       | $O(d \cdot (n + r))$ | $O(d \cdot (n + r))$ | $O(d \cdot (n + r))$ | $O(n + r)$  | 稳定  |

# 外排序计算

**多路归并的最佳归并树**
- 以顺串长度为权重，构建 $k$ 叉 Huffman 树
- 需要**补充 $m$ 个空串**，使得最后正好剩下一个数（每个内部节点都有 $k$ 个子节点），即使得 $(n+m)\%(k-1)=1$ 
- 计算访外次数时要**将 WPL 乘以 2** ，对应读取+写入

**访问外存次数分析（读取+写入）**
- 置换选择排序时，对于 $n$ 个记录，需要 $2n$ 次访外
- 归并时，如果进行了 $m$ 趟归并，需要 $m\cdot (2n)$ 次访外（其中 $m=\log_k R$ ，$k$ 代表 $k$ 路归并，$R$ 为初始顺串数）

**败者树的时间复杂度**
- 原始方法：找到每一个最小值的时间是 $O(k)$，产生一个大小为 n 的顺串的总时间是 $O(k \cdot n)$
- 败者树：初始化包含 k 个选手的败者树需要 $O(k)$ 的时间，读入一个新值并重构的时间为 $O(\log k)$ ，故产生大小为 $n$ 的顺串的总时间为 $Θ(k + n \cdot \log k)$

# B 树

## 性质

$m$ 阶 B 树的要求
- **区间对应关系**：父节点有 $k$ 个值，其有 $k+1$ 个子节点，且子节点的值与父节点各值划分的区间对应
- **上界**：每个节点最多有 $m$ 个子节点（每个节点最多存 $m-1$ 个数）
- **下界**：根节点至少有 1 个数，其它非叶节点至少有 $\lceil m/2 \rceil-1$ 个数
- **叶节点都位于同一层**，且存着 $\lceil m/2 \rceil-1 \sim m-1$ 个关键码

效率
- 构建：$O(n\log n)$ 
- 查找 / 插入 / 删除：$O(\log n)$ 

## 插入删除

**插入**
- 先放到最底层的一个节点（进行一次查找来确定位置）
- 如果节点存储的关键码个数溢出，则进行分裂，把中间的关键码放到父节点
- 如果父节点也溢出，继续分裂；如果根节点也溢出，就向上增加一层

**删除**
- 将删除的数与其后继换位，把这个数搞到叶节点，然后删除这个数
- 如果这个叶节点相邻兄弟能借，就借一个（兄弟给父节点一个，父节点给叶节点一个）
- 如果兄弟都不能借，就从父节点借，然后合并左 / 右兄弟；如果借完了父节点下溢，就向上递归

## 相关计算

**读盘写盘次数**
- 读盘次数：经过的节点数
- 写盘次数：在修改后的 B 树中，和原先不同的节点数量（比如一个节点分裂成 2 个节点，和原来相比，新增 2 分裂出的节点 + 传递一个值给父节点，共 3 次修改）
- 注意分析插入、删除的读写次数时，需要算上一开始查找过程的

数学性质
- 若有 $N$ 个关键码，则空指针数量为 $N+1$ 
- 第 0 层根至少 1 个节点，第 $i$ 层至少 $2 \cdot \lceil m / 2 \rceil^{i-1}$ 个节点（$i\geq1$）
- 查找时最大读盘次数：$k \leq 1 + \log_{\lceil m / 2 \rceil} \left(\frac{N + 1}{2}\right)$

# B+树

$m$ 阶 B+ 树的要求
- **区间对应关系**：父节点有 $k$ 个值，其有 $k$ 个子节点，且子节点的值与父节点各值划分的区间对应
- **上界**：每个节点最多有 $m$ 个子节点（每个节点最多存 $m$ 个数）
- **下界**：根节点至少有 1 个数，其它非叶节点至少有 $\lceil m/2 \rceil$ 个数
- **叶节点都位于同一层**，且存着 $\lceil m/2 \rceil \sim m$ 个关键码
- 内部节点不存储实际关键码，只是便于定位

**插入**：与 B 树类似，需要保证父节点有分裂出的节点的最大关键码

**删除**：与 B 树有以下区别
- 下溢边界调整为 $\lceil m/2 \rceil$ 
- 合并时，不能直接从父节点借数，因为父节点不存储实际关键码，所以**直接合并**两个节点，然后根据合并后的节点的最大值调整父节点
- 删除后，可以沿着到根节点的路径回溯，调整每个节点（因为子节点最大值可能改变），不调整也行（因为子节点仍处在对应区间内，但一般还是会调整的）

# 红黑树

## 性质

红黑树的要求
- 是 BST ，且是扩充二叉树（所以是满二叉树）
- **根叶均黑**：根节点和叶节点（空节点 NIL）都是黑色
- **红红限制**：不允许两个红色节点直接相连
- **黑高一致**：从任意节点出发的每个外部路径，经过的黑色节点数量相同

**黑高 / 阶**：从一个节点出发到叶节点，经过的黑色节点的数量（**不包括自身**）

$k$ 阶红黑树的性质
- 从根到叶的简单路径长度（边数）范围是 $[k,2k]$ ，树高范围是 $[k+1,2k+1]$
- 内部节点最少是 $2^k-1$ （完全满二叉树）
- 内部节点数量为 $n$ 的红黑树，其高度满足 $h\leq 2\log_2(n+1)+1$ 

效率
- 构建：$O(n\log n)$ 
- 查找 / 插入 / 删除：$O(\log n)$ 

## 插入

插入情况
- 双红调整 1 ：父为红，父兄为黑，进行一次 / 两次旋转+变色（终止）
- 双红调整 2 ：父为红，父兄为红，进行变色，向上递归

双红调整 1 ：插入节点 X 的父节点 A 是红色，A 的兄弟节点是黑色

![[数算/img/img11/image-5.png]]

双红调整 2 ：父节点 A 和 A 的兄弟节点都是红色，可以进行换色处理（如果 B 是根节点，则仍保持黑色）

![[数算/img/img11/image-7.png]]

## 删除

删除情况
- 双黑调整 1 ：兄为红，进行一次旋转+变色，转换为情况 3 
- 双黑调整 2 ：兄为黑，兄全黑子（2叶），进行变色，如果父原来为黑，需要向上递归
- 双黑调整 3 ：兄为黑，兄有红子，进行一次 / 两次旋转+变色（终止）

双黑调整 1 ：如果待删节点的兄弟节点是红色，进行旋转并变色，而后转变为双黑调整 3 的情况

![[数算/img/img11/image-12.png]]

双黑调整 2 ：如果待删除节点的兄弟节点是黑色，且连着 2 个叶节点，把兄弟节点设为红色，父节点设为黑色

![[数算/img/img11/image-13.png]]

双黑调整 3 ：如果待删除节点的兄弟节点是黑色，且有红色子节点，需要根据具体情况进行旋转 / 提升操作

![[数算/img/img11/image-14.png]]

![[数算/img/img11/image-15.png]]

# AVL

## 性质

AVL 的要求
- 是 BST
- **高度差限制**：任意节点左右子树高度差最多为 1 

**平衡因子** $bf(x)$ ：右子树高度 - 左子树高度

## 插入

先进行一次（失败的）查找，确定插入位置，如果导致失衡，需要进行平衡操作，从插入位置出发，向上找到第一个失衡的节点，记为 A

插入情况
- LL 型：插入到 A 的左子树的左子树导致失衡，此时 $bf(A)=-2$ 
- LR 型：插入到 A 的左子树的右子树导致失衡，此时 $bf(A)=-2$ 
- RL 型：插入到 A 的右子树的左子树导致失衡，此时 $bf(A)=2$ 
- RR 型：插入到 A 的右子树的右子树导致失衡，此时 $bf(A)=2$ 

调整操作
- LL 和 RR ：旋转
- LR 和 RL ：提升（双旋转）


![[数算/img/img12/image-19.png]]

![[数算/img/img12/image-20.png]]

# 伸展树

## 伸展操作

需要通过一系列旋转，把 x 移动到根节点
- 单旋转：当 x 是树根的孩子时，进行单旋转往上移动 1 层，这就到了根节点
- 双旋转：其它情况下，进行双旋转，把 x 往上移动 2 层；分为一字型和之字形（提升），下图为一字型

![[数算/img/img12/image-27.png]]

## 性质

伸展树不是一个新的数据结构，只是对 BST 的一些改进规则，能让访问的总代价降低，但不能保证树高平衡

在查找、插入、删除时，伸展树会额外进行以下操作
- 查找 x 
	- 如果找到了 x ，通过伸展操作把 x 变到树根
	- 如果没找到，把查找路径上最后访问的节点伸展到树根
- 插入 x ：先按照 BST 的方法插入进去，而后通过伸展操作把 x 变到树根
- 删除 x ：先查找 x （这会让 x 伸展到树根），而后删除这个节点
	- 如果 x 变为树根后没有左 / 右子树，那就直接结束
	- 否则需要把左子树中的最大节点伸展到左子树的根部（由于是左子树中最大的，所以伸展后其没有右子树），并把 x 的右子树接上去

效率
- 不能保证单次访问效率很高，仍为 $O(n)$ 
- 可以保证 m 次操作耗时 $O(m\log n)$ ，即平均效率较高，为 $O(\log n)$ 

## 基于伸展树的区间操作

区间提取：给定序列 $n_1,\cdots,n_m$ ，要找到处在 $[n_a,n_b]$ 中的数
- 根据序列构建伸展树
- 把 $n_a$ 的前驱转移到树根
- 把 $n_b$ 的后继转移到树根的右孩子
- 转移后 $n_b$ 的左子树即为相应区间，对其中序遍历，即可得到相应子序列

区间删除：先进行区间提取，然后删掉对应的子树

区间插入：在序列 A 中 $n_a$ 和 $n_{a+1}$ 之间插入序列 B 
- 根据序列 A 构建伸展树
- 把 $n_a$ 转移到树根
- 把 $n_{a+1}$ 转移到树根的右孩子（由于二者相邻，所以没有比 $n_{a}$ 大但是比 $n_{a+1}$ 小的数，此时 $n_{a+1}$ 没有左子节点）
- 把 B 构建成一个伸展树，作为 $n_{a+1}$ 的左子树

区间翻转：翻转一个序列中的 $[n_a,n_b]$ 的部分
- 先进行区间提取，找到对应区间的子树
- 对这个子树的每个节点，交换它的左右儿子，而后对整个树进行中序遍历即可得到翻转结果

## 半伸展树

对 x 的父节点 y 进行操作，而非对 x 本身

![[数算/img/img12/image-28.png]]

整个过程如下（如果“当前节点”是树根的孩子，就和原来一样进行单旋转，把“当前节点”移到树根）

![[数算/img/img12/image-29.png]]

# 补充计算

**平均检索长度 ASL**
- **成功**：在表中找到目标所需的平均比较次数
- **失败**：在表中确认目标“不存在”所需的平均比较次数，注意相比成功时检索次数需不需要+1

**广义表头尾操作**
- **表头**：第一个元素 $x_0$ ，头运算 `head()` 取出表头
- **表尾**：剩余部分的元素组成的表 $(x_1,\cdots,x_n)$ ，尾运算 `tail()` 取出表尾

